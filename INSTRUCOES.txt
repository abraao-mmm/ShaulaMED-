1 - Instalar Python.

2 - descompatar a pasta do projeto.

Abrir o terminal na pasta.

python -m venv venv

.\venv\Scripts\Activate.ps1

pip install -r requirements.txt

Iniciar a API (uvicorn api:app) num terminal.

Iniciar a Interface (streamlit run app.py) noutro terminal.